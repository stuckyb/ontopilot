# Simple test configuration file.
#

[Ontology]
# The location and name of the compiled ontology OWL file (REQUIRED).  Can be
# either a relative or an absolute path.
ontology_file = ontology/ontname.owl

# The IRI (ID) for the compiled ontology.
ontologyIRI = https://a.sample.iri/to/ontology/ontname.owl

# Ontology terms source CSV files (REQUIRED)
termsfiles = terms_1.csv, , terms_2.csv

[Build]

[Imports]

